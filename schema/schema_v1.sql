CREATE TABLE b2b_logistics_clean_data (
   update_timestamp TIMESTAMP,
   shipment_number VARCHAR,
   order_number VARCHAR,
   order_type VARCHAR,
   order_code VARCHAR,
   line_number INTEGER,
   revision_reason VARCHAR,
   hold_credit VARCHAR,
   sold_to VARCHAR,
   sold_to_name VARCHAR,
   customer_po VARCHAR,
   parent_number VARCHAR,
   item_number VARCHAR,
   description TEXT,
   quantity INTEGER,
   unit_of_measure VARCHAR,
   unit_price_gbp NUMERIC,
   price_unit_of_measure VARCHAR,
   extended_amount_gbp NUMERIC,
   requested_date DATE,
   promised_delivery_date DATE,
   original_promised_time TIME,
   order_date DATE,
   scheduled_pick_date DATE,
   shipment_date DATE,
   delivery_date DATE,
   last_status VARCHAR,
   next_status VARCHAR,
   shipment_status VARCHAR,
   transport_mode VARCHAR,
   carrier_name VARCHAR,
   tracking_number VARCHAR,
   shipping_cost_gbp NUMERIC,
   origin_warehouse_id VARCHAR,
   origin_warehouse_location VARCHAR,
   destination_city VARCHAR,
   destination_postal_code INTEGER,
   customer_contact_number BIGINT,
   customer_email VARCHAR,
   special_instructions TEXT,
   return_status VARCHAR,
   return_reason TEXT,
   employee_id VARCHAR,
   employee_name VARCHAR,
   order_source VARCHAR,
   warehouse_temperature NUMERIC,
   co2_emissions NUMERIC,
   packaging_type VARCHAR,
   insurance_value_gbp NUMERIC
);
